#include "Drawer.h"

// Drawer::Drawer(int img_width, int img_height) {
//     assert(img_width > 0 && img_height > 0);
//     imgWidth = img_width;
//     imgHeight = img_height;
// }

// int Drawer::getImgWidth(){ return imgWidth; }

// int Drawer::getImgHeight(){ return imgHeight; }